{% set name = "PyLD" %}
{% set version = "0.8.2" %}
{% set sha256 = "f11b8586d2d2bc310739a9c49018574d01b8adc4533e950f64c85a13909d7630" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  script: python setup.py install

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  imports:
    - {{ name|lower }}
    
about:
  home: https://pypi.python.org/pypi/{{ name }}/{{ version }}
  license: BSD-3-Clause
  summary: 'Python implementation of the JSON-LD API'
  license_family: BSD
  license_file: LICENSE
  dev_url: https://github.com/digitalbazaar/{{ name|lower }}

extra:
  recipe-maintainers:
    - notestaff
