{% set name = "PyLD" %}
{% set version = "2.0.0" %}
{% set sha256 = "0a72642bfa0a7d22ed538aab991cee113adc87af9e889197eafb837c2deb5dae" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  imports:
    - {{ name|lower }}
    - {{ name|lower }}.documentloader

about:
  home: https://pypi.python.org/pypi/{{ name }}/{{ version }}
  license: BSD-3-Clause
  summary: Python implementation of the JSON-LD API
  license_family: BSD
  license_file: LICENSE
  dev_url: https://github.com/digitalbazaar/{{ name|lower }}

extra:
  recipe-maintainers:
    - notestaff
